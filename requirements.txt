# Phase 1 (dataset inspection + EDA). Do not install unused stacks "just in case".
pandas>=2.2,<3
numpy>=1.26,<3
openpyxl>=3.1,<4
matplotlib>=3.8,<4
seaborn>=0.13,<0.14
jupyter>=1.0,<2

# Dataset download (optional). Requires a Kaggle account.
kagglehub>=0.3,<1

# Later phases (uncomment when we reach that step):
scikit-learn>=1.5
# imbalanced-learn>=0.12
 xgboost>=2.0
shap>=0.46
# fastapi>=0.115
# uvicorn>=0.30
pytest>=8